type StoragePath {
    id: ID!
    storageId: String!
    type: StorageType!
    urls: String!
    weight: Int!
    maxStorage: Int!
    canSeal: Boolean!
    canStore: Boolean!
    groups: String
    allowTo: String
    allowTypes: String
    denyTypes: String
    capacity: Int!
    available: Int!
    fsAvailable: Int!
    reserved: Int!
    used: Int!
    lastHeartbeat: Time!
    heartbeatErr: String
    allowMiners: String!
    denyMiners: String!
}
