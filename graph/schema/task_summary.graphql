type TaskSummary {
    name: String!
    trueCount: Int!
    falseCount: Int!
    totalCount: Int!
}

type TaskSummaryDay {
    day: Time!
    trueCount: Int!
    falseCount: Int!
    totalCount: Int!
}
